{% extends 'base.html.twig' %}


{% block body %}

       {# ----------------------------------------------- image remplace video --------------------------------------#}
<div class="banner_imgFixe">
    <div class="container ">
        <img src="{{ asset('asset/img/banner_odyssey.jpg') }}" alt="">
    </div>
</div>
<div class="container">
    <br>
    <h1>Edition Séjour</h1>

    {{ form_start(form) }}

    {{ form_row(form.titre) }}

    {{ form_row(form.prixSejour) }}

    {{ form_row(form.destination) }}

    {{ form_row(form.typeHebergement) }}

    {{ form_row(form.dureedata) }}

    {{ form_row(form.moisDepart) }}

    {{ form_row(form.description) }}

    {{ form_row(form.description_detail) }}

    {{ form_row(form.promo) }}

    {% if original_image1 is not null %}
        <p>
            Image actuelle :
            <img  src="{{ asset('images/' ~ original_image1) }}" height="200px">
        </p>
    {% endif %}

    {{ form_row(form.image_1) }}

    {% if original_image2 is not null %}
        <p>
            Image actuelle :
            <img  src="{{ asset('images/' ~ original_image2) }}" height="200px">
        </p>
    {% endif %}

    {{ form_row(form.image_2) }}

    {{ form_row(form.vedette) }}

    {{ form_row(form.teleportation) }}

{#    {{ form_row(form.stock) }}#}



    <div class="text-right">
        <button type="submit" class="btn btn-secondary">
            Enregistrer
        </button>
        <a href="{{ path('app_admin_sejour_index') }}" class="btn btn-outline-secondary">
            Retour
        </a>
    </div>

    {{ form_end(form) }}

{% endblock %}

{% block javascripts %}
    {{ parent ()  }}
    <script src = "https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.min.js"></script>
    <script>
        $(document).ready(function () {
            bsCustomFileInput.init()
        });
    </script>

</div>
<br>
<br>
{% endblock %}


